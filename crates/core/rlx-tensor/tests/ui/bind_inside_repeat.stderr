error: `bind` is not allowed inside `repeat` — adopt outer tensors before the loop
  --> tests/ui/bind_inside_repeat.rs:10:18
   |
10 |             bind w;            // `bind` adopts a name — not allowed inside a loop
   |                  ^
