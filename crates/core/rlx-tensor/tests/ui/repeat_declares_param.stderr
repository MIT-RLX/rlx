error: cannot declare an `input`/`param`/`const` inside `repeat` (its name would collide each iteration) — declare it outside
  --> tests/ui/repeat_declares_param.rs:10:19
   |
10 |             param w: [4, 4];   // would collide each iteration
   |                   ^
